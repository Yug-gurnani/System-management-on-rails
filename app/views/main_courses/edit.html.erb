<main>
<br/>
<br/>
<br/>
<br/>
  <div class = "container">
    <% if current_admin.admin %>
      <div class="card">
        <div class="card-header">
          <h1>Edit Course</h1>
        </div>
        <div class="card-body">

        <%= render 'form', main_course: @main_course %>

        <%= link_to 'Show', @main_course %> |
        <%= link_to 'Back', main_courses_path %>
        </div>
      </div>
    <% else %>
      <h1 style = "color:white;">Not Authorised</h1>
    <% end %>
  </div>
</main>
